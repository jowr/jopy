# 2015-07-14 20:12 CAUTION: This file is automatically generated from meta.yaml.tpl, do not edit it manually
package:
  name: jopy
  version: 0.0.1

source:
  git_tag: 
  git_url: https://github.com/jowr/jopy.git

  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

build:
  script: python setup.py install

requirements:
  build:
    - python
    - setuptools
    - numpy
    - scipy
    - matplotlib
    - pandas
    - pip
    - pyyaml
    - nose
    - sphinx
    - jinja2
    - coolprop
    - texttable
    - brewer2mpl
    - future
    - coveralls
    - nose-cov
    - codecov
    - tox

  run:
    - python
    - numpy
    - scipy
    - matplotlib
    - pandas
    - coolprop
    - texttable
    - brewer2mpl
    - future

test:

  imports:
    - jopy

  commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.
    nosetests --with-coverage --cover-package=jopy --logging-level=INFO

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  requires:
    - numpy
    - scipy
    - matplotlib
    - pandas
    - pip
    - pyyaml
    - nose
    - sphinx
    - jinja2
    - coolprop
    - texttable
    - brewer2mpl
    - future
    - coveralls
    - nose-cov
    - codecov
    - tox

about:
  home: http://www.jorrit.org
  license: MIT License